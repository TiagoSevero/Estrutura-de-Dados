/*

	A diferença conceitual é que v quando expresso por v[3] representa o conteúdo de v na posição 3, isto é, se um vetor é declaro, por exemplo, "v[] = {3,4,5,6,7};" v[3] representa o valor contido na posição 4 (quatro) do vetor, no exemplo em questão v[3] = 6. Já v+3 representa o endereço de memória referente a posição 4 (quatro) do vetor e não o conteúdo do mesmo.
	 


*/
